# xlsxfill tests data_raise

One family per rule that stops the run, one case per way that rule can be
broken, named `error-<rule>-<instance>`.

`error.json` carries the exception's name and the message in full.
